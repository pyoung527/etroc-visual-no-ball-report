apiVersion: v1
kind: Service
metadata:
  name: etl-hybrid-bbqc
  labels:
    app: etl-hybrid-bbqc
spec:
  selector:
    app: etl-hybrid-bbqc
  ports:
    - name: http
      port: 8080
      targetPort: 8080
